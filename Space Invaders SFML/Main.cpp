#include "WindowManager.h"

#pragma comment(linker, "/SUBSYSTEM:windows /ENTRY:mainCRTStartup") // Hides the debug console during gameplay


int main()
{
	WindowManager Game; //Creates the window

	return 0;
}